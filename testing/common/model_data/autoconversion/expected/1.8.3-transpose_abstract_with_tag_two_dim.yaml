flow123d_version: 1.8.3
problem: !SequentialCoupling
  description: Test
  mesh:
    mesh_file: mesh.msh
  primary_equation: !Steady_MH
  
    # Test data
    input_fields:
      # Transposed input_fields to array with two items
      # CORRECT result is:
    # ????????? co kdyz tady bude rid: [1, 2] ???????
      # - anisotropy: !FieldFormula
      #     - [x+y, x-y, z]
      #     - [x*x, y*y, z*z]  
    # ?????????

      - anisotropy: !FieldFormula
          - [x+y, x-y, z]
      - anisotropy: !FieldFormula
          - [x*x, y*y, z*z]      


    # Placeholder data
    solver: !Petsc
        r_tol: 1
        a_tol: 1
    balance: 
      balance_on: true
    output:
      output_stream: 
        file: ./test1.pvd
        format: !gmsh
      output_fields:
        - pressure_diff
